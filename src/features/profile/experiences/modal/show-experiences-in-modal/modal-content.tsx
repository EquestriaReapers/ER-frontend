import { Box, Typography } from "@mui/material";
import { modalStyle } from "../styles/styles";

const ShowExperienceModalContent = () => {
  return (
    <Box sx={modalStyle}>
      <Typography>Mostrar experiencia</Typography>
    </Box>
  );
};

export default ShowExperienceModalContent;
